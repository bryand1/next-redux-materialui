export const initialState = {
  index: true,
};

const main = (state, action) => {
  let newState = {...state};
  switch(action.type) {
    default:
      return newState;
  }
};

export default main;
